<%= form_for(@termunit) do |f| %>
  <% if @termunit.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@termunit.errors.count, "error") %> prohibited this termunit from being saved:</h2>

      <ul>
      <% @termunit.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :cohort %><br />
    <%= f.text_field :cohort %>
  </div>
  <div class="field">
    <%= f.label :term %><br />
    <%= f.text_field :term %>
  </div>
  <div class="field">
    <%= f.label :units %><br />
    <%= f.text_field :units %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
